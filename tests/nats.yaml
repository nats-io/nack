---
global:
  labels:
    app: main-jetstream

natsBox:
  enabled: false

config:
  cluster:
    enabled: false

  gateway:
    enabled: false

  jetstream:
    enabled: true

    memoryStore:
      enabled: true
      maxSize: 256Mi
      pvc:
        enabled: true
        size: 256Mi

---
global:
  labels:
    app: main-jetstream

natsBox:
  enabled: false

config:
  cluster:
    enabled: false

  gateway:
    enabled: false

  jetstream:
    enabled: true

    memStorage:
      enabled: true
      size: 256Mi

    fileStorage:
      enabled: false
